@st.cache_resource
def load_model():
    bundle = joblib.load("model.pkl")
    return (
        bundle["pipeline"],
        bundle["threshold"],
        bundle["final_num"],
        bundle["final_cat"],
    )

model, threshold, final_num, final_cat = load_model()
